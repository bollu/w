#include <assert.h>
#include <algorithm>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <utility>
#include <vector>

// mark-whole-buffer
using namespace std;
using ll = long long;

const ll INFTY = 1e5;
int main() {
    ios::sync_with_stdio(false);
    int t;
    cin >> t;

    while (t--) {
      int n, k; cin >> n >> k;
      vector<int> as(n);
      for(int i = 0; i < n; ++i) {
	cin >> as[i];
      }
      
    }
}

