#include <iostream>
using namespace std;

int as[1000000];
int bs[1000000];

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    int T; cin >> T; // 100

    while(T--) {
        int n, k; cin >> n >> k; // n, k = 100
        for(int i = 0 ; i < n; ++i) cin >> a[i] >> b[i]; 
    }
    return 0;
}
