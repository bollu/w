#include <assert.h>
#include <iostream>
#include <vector>
#include <map>
#include <set>

using namespace std;
int main() { 
    int n, m;
    cin >> n >> m;
    vector<int> tree(2*n+1);
    for(int i = 0; i < n; ++i) {
    }
    return 0;
}
