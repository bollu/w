#include <iostream>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <assert.h>
using namespace std;
using ll = long long;


// f1 = x
// f2 = y
// f(i) = f(i-1) + f(i+1)
// --
// f(i+1) = f(i) - f(i-1)
//
// f3 = y - x
// f4 = (y - x) - y = -x
// f5 = 

int main() {
    cin.tie(nullptr); ios::sync_with_stdio(false);
    return 0;
}

