// segtree: https://codeforces.com/problemset/problem/1234/D
#include<iostream>
#include<utility>
#include<tuple>
#include<algorithm>
#include<assert.h>
#define A assert
#define I int
#define V void
#define R return
#define DO(i, n, x) for(I i=0;i<n;++i) {x;}
#define mt make_tuple
#define N 1e5+2;
using namespace std;
vector<set<int>> a[2*N];

int main() {
    std::ios_base::sync_with_stdio(false);
    char input[N]; cin>>input;
    int qs; cin>>qs;
    while(qs--) {
        int t; cin>> t;
        if (t==0) {
            int pos; char c; cin>>pos>>c;
        } else{
            int l, r; cin>>l>>r;
        }
    }
}
























